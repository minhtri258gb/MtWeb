import React from 'react';
import ReactDOM from 'react-dom/client';
// import PageHome from './pages/home/Home';
import PageMusic from './pages/music/Music';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PageMusic />
  </React.StrictMode>
);